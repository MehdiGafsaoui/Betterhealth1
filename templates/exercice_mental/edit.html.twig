{% extends 'Client/index.html.twig' %}


{% block body %}
<div class="container mt-5 pt-5"> {# Increased margin and padding to move content further down #}
    <div class="d-flex justify-content-center">
        <div class="w-75">
            <h1 class="text-center">Edit Exercice Mental</h1>
            <br><br>
            {{ include('exercice_mental/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="text-center mt-3">
                <a href="{{ path('app_exercice_mental_index') }}" class="btn btn-secondary">Back to List</a>
            </div>

            <div class="mt-3">
                {{ include('exercice_mental/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}


