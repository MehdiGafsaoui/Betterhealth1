{# templates/exercice_mental/show.html.twig #}
{% extends 'admin/admin.html.twig' %}

{% block content %}
<div class="container mt-4">
  <div class="card shadow-sm">
    <div class="card-header bg-info text-white">
      <h2 class="mb-0">Exercice Mental Details</h2>
    </div>
    <div class="card-body">
      <table class="table table-bordered">
        <tbody>
          <tr>
            <th style="width: 150px;">ID</th>
            <td>{{ exercice_mental.id }}</td>
          </tr>
          <tr>
            <th>Video URL</th>
            <td>{{ exercice_mental.videoUrl }}</td>
          </tr>
          <tr>
            <th>Titre</th>
            <td>{{ exercice_mental.titre }}</td>
          </tr>
          <tr>
            <th>Description</th>
            <td>{{ exercice_mental.description }}</td>
          </tr>
          <tr>
            <th>Durée (minutes)</th>
            <td>{{ exercice_mental.dureeMinutes }}</td>
          </tr>
          <tr>
            <th>Objectif</th>
            <td>{{ exercice_mental.objectif }}</td>
          </tr>
        </tbody>
      </table>
    </div>
    <div class="card-footer text-end">
      <a href="{{ path('app_exercice_mental_index') }}" class="btn btn-secondary">Back to list</a>
      <a href="{{ path('app_exercice_mental_edit', {'id': exercice_mental.id}) }}" class="btn btn-warning">Edit</a>
      {{ include('exercice_mental/_delete_form.html.twig') }}
    </div>
  </div>
</div>
{% endblock %}
