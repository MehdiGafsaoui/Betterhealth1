<!DOCTYPE html>

<title>Categorieevenement</title>

{% block body %}
    <h1>Categorieevenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieevenement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorieevenement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorieevenement_index') }}">back to list</a>

    <a href="{{ path('app_categorieevenement_edit', {'id': categorieevenement.id}) }}">edit</a>

    {{ include('categorieevenement/_delete_form.html.twig') }}
{% endblock %}
