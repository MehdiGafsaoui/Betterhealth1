


{% extends 'test/test.html.twig' %}
{% block body %}
<div class="table-container">
<title>CentreDeDon</title>
    <h1>CentreDeDon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centre_de_don.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ centre_de_don.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ centre_de_don.address }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ centre_de_don.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ centre_de_don.email }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ centre_de_don.createdAt ? centre_de_don.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_centre_de_don_index') }}">back to list</a>
{% if 'ROLE_ADMIN' in app.user.roles %}
    <a href="{{ path('app_centre_de_don_edit', {'id': centre_de_don.id}) }}">edit</a>
    {% endif %}


    {{ include('centre_de_don/_delete_form.html.twig') }}
    </div>
    <style>
.table-container {
    margin-top: 100px; /* Adjust according to your header height */
    text-align: center; /* Centers the title */
}

.table {
    margin: auto; /* Centers the table */
}
</style>
{% endblock %}


