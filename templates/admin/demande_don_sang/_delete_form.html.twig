<form method="post" action="{{ path('app_demande_don_sang_delete', {'id': demande_don_sang.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_don_sang.id) }}">
    <button class="btn">Delete</button>
</form>
